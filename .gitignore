# Secrets - NIEMALS committen
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Laufzeit-Artefakte
logs/
data/cache/
backtest_results/
*.log

# Protokoll der taeglichen Datenluecken-Pruefung (rechnerspezifisch, waechst)
datenluecken_log.txt

# Marktdaten-Speicher (ntbridge)
# Waechst dauerhaft, ist reproduzierbar (NinjaTrader liefert die Historie neu)
# und wird im laufenden Betrieb beschrieben - gehoert nicht in die Versionierung.
# Die -shm/-wal-Dateien sind der WAL-Modus von SQLite.
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
*.db

# Lokale Claude-Code-Einstellungen (rechnerspezifisch)
.claude/settings.local.json

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Dukascopy-Naeherungsdaten (waechst auf viele GB, jederzeit neu ladbar)
data/dukascopy_*.sqlite3
data/dukascopy_*.sqlite3-shm
data/dukascopy_*.sqlite3-wal

# Reste des Linux-Mounts: eine geloeschte, aber noch geoeffnete Datei wird von
# FUSE unter diesem Namen weitergefuehrt. Sie gehoert niemandem und darf nicht
# ins Repository.
.fuse_hidden*
data/DUKA_5m.csv
data/DUKA_5m.csv.HERKUNFT.txt

# Node.js
node_modules/
dist/


# Arbeitsdateien einzelner Sitzungen - nie ins Repo.
.scratch/

# Bilder des Musterreferenzsatzes - aus der Kerzenhistorie jederzeit neu zu
# erzeugen. Laurins URTEILE dagegen sind nicht reproduzierbar und liegen
# deshalb als Text in data/w_referenz_urteile.csv, das versioniert wird.
data/w_referenz_bilder/
*.sqlite3-journal

# Zwischenspeicher und Rohtabelle der W-Stufenmessung. Beides ist aus der
# Kerzenhistorie reproduzierbar (werkzeuge/w_stufenmessung.py) und zu gross
# fuer die Versionierung; die Befunde stehen in docs/.
data/w_kandidaten.npz
data/w_stufenmessung.csv
